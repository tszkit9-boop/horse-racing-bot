import { createFileRoute } from '@tanstack/react-router'
import { Bell, KeyRound, Save, ShieldCheck } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Input } from '@/components/ui/input'

export const Route = createFileRoute('/app/settings')({ component: SettingsPage })
function SettingsPage() { return <div className="max-w-3xl space-y-6"><div><h1 className="font-serif text-4xl font-medium tracking-tight">Settings</h1><p className="mt-2 text-sm text-muted-foreground">Shape how your workspace runs and notifies your team.</p></div><Card className="border-border/70"><CardHeader><CardTitle className="flex items-center gap-2 text-base"><ShieldCheck className="size-4 text-primary" /> Workspace profile</CardTitle></CardHeader><CardContent className="space-y-4"><label className="block space-y-2 text-sm font-medium">Workspace name<Input defaultValue="Northstar Operations" /></label><label className="block space-y-2 text-sm font-medium">Workspace slug<Input defaultValue="northstar-ops" /></label><Button><Save className="size-4" /> Save changes</Button></CardContent></Card><div className="grid gap-4 sm:grid-cols-2"><Card className="border-border/70"><CardContent className="p-5"><Bell className="size-5 text-primary" /><p className="mt-4 text-sm font-semibold">Notifications</p><p className="mt-1 text-xs leading-5 text-muted-foreground">Get notified when a bot restarts or needs attention.</p></CardContent></Card><Card className="border-border/70"><CardContent className="p-5"><KeyRound className="size-5 text-primary" /><p className="mt-4 text-sm font-semibold">Secrets & access</p><p className="mt-1 text-xs leading-5 text-muted-foreground">Manage bot tokens with isolated, encrypted storage.</p></CardContent></Card></div></div> }
